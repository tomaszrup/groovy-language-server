{
  "name": "groovy-spock-support",
  "displayName": "Groovy & Spock Language Support",
  "description": "Language support for Groovy and Spock",
  "version": "0.1.16",
  "publisher": "TomaszRup",
  "homepage": "https://github.com/tomaszrup/groovy-language-server",
  "repository": {
    "type": "git",
    "url": "https://github.com/tomaszrup/groovy-language-server.git"
  },
  "bugs": {
    "url": "https://github.com/tomaszrup/groovy-language-server/issues"
  },
  "license": "Apache-2.0",
  "icon": "./images/groovy.png",
  "categories": [
    "Programming Languages"
  ],
  "keywords": [
    "Groovy",
    "Grails",
    "Spock",
    "Spock Framework",
    "Testing"
  ],
  "main": "./build/extension.js",
  "engines": {
    "vscode": "^1.99.0"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/vscode": "^1.99.0",
    "vitest": "^3.1.1",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.3",
    "@vscode/vsce": "^3.4.2",
    "vscode-jsonrpc": "^8.2.1",
    "vscode-languageclient": "^9.0.1",
    "webpack": "^5.99.9",
    "webpack-cli": "^6.0.1"
  },
  "activationEvents": [
    "onLanguage:groovy"
  ],
  "contributes": {
    "languages": [
      {
        "id": "groovy",
        "extensions": [
          ".groovy",
          ".gvy",
          ".gy",
          ".gsh",
          ".gradle"
        ],
        "filenames": [
          "Jenkinsfile"
        ],
        "aliases": [
          "Groovy"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "groovy",
        "scopeName": "source.groovy",
        "path": "./syntaxes/groovy.tmLanguage.json"
      }
    ],
    "semanticTokenScopes": [
      {
        "language": "groovy",
        "scopes": {
          "namespace": ["entity.name.type.module.groovy"],
          "type": ["entity.name.type.groovy"],
          "class": ["entity.name.type.class.groovy"],
          "interface": ["entity.name.type.interface.groovy"],
          "enum": ["entity.name.type.enum.groovy"],
          "typeParameter": ["entity.name.type.type-parameter.groovy"],
          "parameter": ["variable.parameter.groovy"],
          "variable": ["variable.other.local.groovy"],
          "property": ["variable.other.property.groovy"],
          "function": ["entity.name.function.groovy"],
          "method": ["entity.name.function.member.groovy"],
          "decorator": ["entity.name.function.decorator.groovy"],
          "enumMember": ["variable.other.enummember.groovy"],
          "keyword": ["keyword.other.groovy"]
        }
      }
    ],
    "commands": [
      {
        "command": "groovy.restartServer",
        "title": "Restart Groovy language server",
        "category": "Groovy"
      },
      {
        "command": "groovy.showOutputChannel",
        "title": "Show Output Channel",
        "category": "Groovy"
      }
    ],
    "configuration": {
      "type": "object",
      "description": "Groovy configuration",
      "properties": {
        "groovy.java.home": {
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "Specifies the folder path to the JDK. Use this setting if the extension cannot find Java automatically."
        },
        "groovy.java.vmargs": {
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "Extra JVM arguments for the Groovy language server (e.g. \"-Xmx2g\"). When unset, heap is auto-sized (512 MB–4 GB) based on workspace size, G1GC is enabled, and HeapDumpOnOutOfMemoryError is set."
        },
        "groovy.classpath": {
          "type": "array",
          "default": null,
          "description": "Specifies additional entries to add to the classpath. May contain both folders and individual .jar files.",
          "items": {
            "type": "string"
          }
        },
        "groovy.debug.serverPort": {
          "type": "number",
          "default": 0,
          "description": "If set, connect to an existing Groovy LSP server on this TCP port instead of starting one automatically."
        },
        "groovy.logLevel": {
          "type": "string",
          "default": "INFO",
          "enum": ["ERROR", "WARN", "INFO", "DEBUG", "TRACE"],
          "description": "Set the log level for the Groovy language server. Lower levels produce more output. Changing this restarts the server."
        },
        "groovy.showMemoryUsage": {
          "type": "boolean",
          "default": false,
          "description": "Show the language server's JVM memory usage in the status bar next to \"Groovy\"."
        },
        "groovy.semanticHighlighting.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable semantic syntax highlighting for Groovy files."
        },
        "groovy.formatting.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable document formatting for Groovy files."
        },
        "groovy.maven.home": {
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "Specifies the folder path to the Maven installation. Use this if mvn is not on your PATH."
        },
        "groovy.project.importers": {
          "type": "array",
          "default": [],
          "description": "Limit which build-tool importers are active during project discovery. Valid values: \"Gradle\", \"Maven\". An empty array (default) enables all importers. Set to [\"Gradle\"] to skip Maven discovery entirely, or [\"Maven\"] to skip Gradle.",
          "items": {
            "type": "string",
            "enum": ["Gradle", "Maven"]
          }
        },
        "groovy.memory.scopeEvictionTTL": {
          "type": "number",
          "default": 300,
          "minimum": 0,
          "description": "Time in seconds after which an inactive project scope's heavy state (AST, classloader, compilation unit) is evicted from memory. The scope is transparently reactivated on the next request. Set to 0 to disable eviction. Useful for large multi-project workspaces."
        },
        "groovy.memory.backfillSiblingProjects": {
          "type": "boolean",
          "default": false,
          "description": "When true, opening a file in one Gradle subproject automatically resolves classpaths for all sibling subprojects in the background. This improves cross-project navigation but increases memory usage. When false (default), each project's classpath is resolved on demand when you first open a file in it."
        },
        "groovy.memory.perProjectMB": {
          "type": "number",
          "default": 128,
          "minimum": 32,
          "maximum": 512,
          "description": "Memory (in MB) allocated per project when auto-sizing the heap. With scope eviction enabled (default), up to 8 active projects use this allocation. Without eviction, all discovered projects count. Default: 128 MB."
        },
        "groovy.memory.pressureThreshold": {
          "type": "number",
          "default": 75,
          "minimum": 30,
          "maximum": 95,
          "description": "Heap usage percentage at which the language server triggers emergency scope eviction. When the JVM heap exceeds this threshold, the least-recently-used inactive scope is evicted immediately regardless of the TTL timer. Lower values are more aggressive (evict sooner). Default: 75%."
        }
      }
    }
  }
}
